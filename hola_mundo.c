#include <stdio.h>
//le dice al compilador que incluya el contenido de stdio (standard input, output).

int main(void){
	//declaramos la funcion principal del programa
	printf("¡Hola, mundo!\n");
	//imprime con salto de linea al final
	return 0;
	//si se hace return 0 entonces el programa acabó sin errores.s
}